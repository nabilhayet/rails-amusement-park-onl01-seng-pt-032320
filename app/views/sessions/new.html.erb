<%= form_for @user, url: '/signin' do |f| %>
    <%= f.label :name %>
    <%= f.collection_select(:name, User.all, :id, :name) %>
    <br>
    <br>
    <%= f.label :password %>
    <%= f.number_field :password %> 
    <br>
    <%= f.button "Sign In" %>
    <% end %>